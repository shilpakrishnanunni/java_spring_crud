info:
  name: PATCH patchUser
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:8080/users/1
  body:
    type: json
    data: |-
      {
        "email": "john222@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
