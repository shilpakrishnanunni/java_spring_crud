info:
  name: GET Query Param
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/hello?name=shilpa
  params:
    - name: name
      value: shilpa
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
